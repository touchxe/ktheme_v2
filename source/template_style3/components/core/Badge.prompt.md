상태나 분류를 표시하는 작은 라벨. 실시간 예배 표시, 설교 카테고리, 메타 태그에 사용.

```jsx
<Badge variant="live" />
<Badge variant="category" category="수요">수요예배</Badge>
<Badge variant="soft">VOL 04</Badge>
```

- `live`: 레드 사각 뱃지, LIVE 전용. `category`: 분류 색 자동 매핑(주일/수요/청년/새벽/유년부/기타).
- `solid`/`soft`/`outline`: 일반 라벨. 이모지는 쓰지 않음.
